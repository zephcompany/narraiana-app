import type {Project} from './mapping-data';
function loadImage(src:string):Promise<HTMLImageElement>{return new Promise((resolve,reject)=>{const im=new Image();im.onload=()=>resolve(im);im.onerror=()=>reject(new Error('Não foi possível carregar uma imagem. Tente novamente.'));im.src=src})}
export async function renderComposition(p:Project,photo:string,original=false){const base=await loadImage(photo);const canvas=document.createElement('canvas');const logicalHeight=1000*p.photoHeight/p.photoWidth;const scale=Math.min(1.6,2200/Math.max(1000,logicalHeight));canvas.width=Math.round(1000*scale);canvas.height=Math.round(logicalHeight*scale);const ctx=canvas.getContext('2d')!;ctx.fillStyle='#fff';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.drawImage(base,0,0,canvas.width,canvas.height);if(original)return canvas;ctx.scale(scale,scale);for(const l of p.layers){ctx.save();ctx.globalAlpha=l.opacity;ctx.translate(l.x,l.y);ctx.rotate(l.rotation*Math.PI/180);if(l.kind==='lash'&&l.src){const image=await loadImage(l.src);if(l.flip)ctx.scale(-1,1);ctx.drawImage(image,-l.width/2,-l.height/2,l.width,l.height)}else if(l.kind==='text'){ctx.fillStyle=l.color;ctx.font=`${l.height}px Arial`;ctx.textAlign='center';ctx.textBaseline='middle';ctx.strokeStyle=l.color==='#ffffff'?'#111':'#fff';ctx.lineWidth=.7;ctx.strokeText(l.text||'',0,0);ctx.fillText(l.text||'',0,0)}else if(l.kind==='stroke'){ctx.strokeStyle=l.color;ctx.lineWidth=l.strokeWidth||3;ctx.lineCap='round';ctx.lineJoin='round';ctx.beginPath();l.points?.forEach((v,i)=>i===0?ctx.moveTo(v.x,v.y):ctx.lineTo(v.x,v.y));ctx.stroke()}ctx.restore()}return canvas}
export function downloadBlob(blob:Blob,name:string){const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),60000)}
